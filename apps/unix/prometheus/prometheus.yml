global:
  scrape_interval: 15s

scrape_configs:
  - job_name: node
    static_configs:
      - targets: ["localhost:9100"]   # node_exporter (this Mac)

  - job_name: rpi
    static_configs:
      - targets: ["pi.local:9101"]   # rpi-exporter (Raspberry Pi)
